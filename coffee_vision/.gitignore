# runtime artifacts
events.db
*.pt
out_*.mp4
__pycache__/
*.pyc
.venv/
venv/

# large/generated media — keep the repo light. Regenerate fixtures with
#   python -m scripts.make_synthetic        # shapes crash-test
#   python -m scripts.make_motion_fixture   # public-image motion fixture
# Drop your own real clip at data/sample.mp4 (see README).
data/synthetic_test.mp4
data/motion_fixture.mp4
data/sample.mp4

# cached downloads (test image for the motion fixture)
.cache/

# ad-hoc video intake + processing outputs
incoming/
outputs/

# training data & artifacts (your footage / large / machine-specific)
training/dataset/images/
training/dataset/labels/
training/dataset/data.yaml
training/runs/
models/*.pt
models/active.txt

# user-supplied videos and generated run outputs
incoming/
outputs/
