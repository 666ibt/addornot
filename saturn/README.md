# Saturn — десктоп-приложение

Набор инструментов для документов (главное меню):

1. **Обработка ТТН** — распознаёт накладные ГСМ и разбивает PDF по страницам,
   имя `{накладная}_{договор}.pdf`.
2. **Разделитель PDF** — делит PDF по страницам, имя по умолчанию
   `{имя}_splitted_1,2,3…`, редактируется вручную.
3. **Фото → PDF** — собирает JPG/PNG в PDF (в один файл или по одному).
4. **Определитель листов согласования** — читает шапку и формирует имя
   `Лист согласований к Договору № … от … Контрагент`.

Ниже — про инструмент №1 (обработка ТТН).

---

Разбивает многостраничные PDF-сканы топливных накладных (ТТН / ГСМ) на
**по одному PDF на страницу** и называет каждый файл по номеру накладной и
договора в виде `{накладная}_{договор}.pdf` (например `37_SGN-2-25.pdf`).

Поддерживаются два формата:

- **Товарно-транспортная накладная** (типовая форма № 1-т);
- **«Накладная на отпуск материалов»** Ферганского НПЗ (ФНПЗ).

Номер договора начинается с `SGN` (например `SGN-158/25`, `SGN-59/26-P`);
для внутренних перемещений вместо номера подставляется `Перемещение`.

Это настольная реализация навыка `/ttn-waybill-splitter`.

---

## Как это работает

1. **Растеризация** — каждая страница PDF превращается в изображение
   (`pdf-to-img`).
2. **Гибридное распознавание**:
   - сначала локальный **OCR** (`tesseract.js`, русский + английский) и разбор
     номеров регулярными выражениями (`src/main/extract.js`);
   - если уверенность низкая — **резервное распознавание через Claude**
     (vision-модель) по изображению страницы (`src/main/ai.js`);
   - всё это можно **вручную поправить** в интерфейсе перед сохранением.
3. **Разбивка** — исходная страница копируется в отдельный PDF **без потери
   качества** (`pdf-lib`, не сохраняем растровую картинку) и получает имя
   `{накладная}_{договор}.pdf`. Совпадающие имена автоматически получают
   суффиксы `_2`, `_3`, …

## Стек

- **Electron** — десктоп-оболочка (Windows / macOS / Linux);
- **pdf-lib** — разбивка PDF постранично;
- **pdf-to-img** — рендер страниц в изображения;
- **tesseract.js** — локальный OCR (офлайн);
- **Claude API** — резервное распознавание сложных сканов;
- **electron-store** — локальные настройки (API-ключ хранится только на
  вашем компьютере).

## Запуск в разработке

```bash
cd saturn
npm install
npm start
```

## Сборка установщика

```bash
npm run dist        # для текущей ОС
npm run dist:win    # Windows: portable .exe + установщик NSIS
npm run dist:mac    # macOS (.dmg)
npm run dist:linux  # Linux (AppImage)
```

Готовые файлы появятся в папке `release/`. Для Windows собираются два файла:

- `TTN-Waybill-Splitter-<версия>-portable.exe` — **портативная** версия,
  запускается без установки;
- `TTN Waybill Splitter Setup <версия>.exe` — обычный установщик.

> Готовые сборки для Windows также автоматически публикуются в разделе
> [Releases](https://github.com/666ibt/addornot/releases) через GitHub Actions.

## Настройка AI-распознавания (необязательно, но рекомендуется)

Откройте **⚙ Настройки** в приложении и вставьте свой **Anthropic API-ключ**.
Там же можно выбрать модель и когда подключать AI:

- *только при низкой уверенности* (по умолчанию, дешевле);
- *при низкой и средней уверенности*;
- *никогда* — полностью офлайн, только локальный OCR.

Ключ хранится локально через `electron-store` и **никогда не попадает в
репозиторий**.

## Офлайн-OCR

Языковые данные (`rus` + `eng`) **встроены** в приложение — они лежат в
папке `tessdata/` и включаются в сборку через `extraResources`. Приложение
распознаёт текст полностью офлайн и **не обращается к интернету**, что важно
в сетях с SSL-инспекцией (самоподписанные сертификаты). Модуль `ocr.js`
находит эти файлы автоматически (в собранном приложении — в `resources/`,
в режиме разработки — в корне проекта).

## Тесты

Ядро разбора номеров (`src/main/extract.js`) покрыто юнит-тестами и не
зависит от Electron:

```bash
npm test
```

## Структура

```
saturn/
├── package.json
├── src/
│   ├── main/            # процесс Electron (Node)
│   │   ├── main.js      # окно, IPC, конвейер обработки
│   │   ├── preload.js   # безопасный мост в интерфейс
│   │   ├── pdf.js       # рендер и разбивка PDF
│   │   ├── ocr.js       # tesseract.js
│   │   ├── ai.js        # резерв через Claude
│   │   ├── extract.js   # разбор накладной/договора (чистая логика)
│   │   └── settings.js  # локальные настройки
│   └── renderer/        # интерфейс (HTML/CSS/JS)
│       ├── index.html
│       ├── renderer.js
│       └── styles.css
└── test/
    └── extract.test.js
```
